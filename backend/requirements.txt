# backend/requirements.txt — NeuroSpell AI P300 EEGNet Backend
# Install: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9       # Required for form data in FastAPI

# ── Core scientific stack ──────────────────────────────────────────
numpy>=1.24
scipy>=1.10
h5py>=3.8                     # HDF5 / MATLAB v7.3 .mat files

# ── Deep learning ─────────────────────────────────────────────────
torch>=2.0                    # PyTorch — EEGNet inference engine
# Note: For CUDA acceleration install the matching CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# ── Machine learning (legacy LDA path) ───────────────────────────
scikit-learn>=1.3
joblib>=1.3                   # Model serialisation (.pkl)

# ── LLM integrations (optional but recommended) ───────────────────
openai>=1.30                  # Primary LLM backend (GPT-3.5 / GPT-4o-mini)
litellm>=1.40                 # Secondary LLM proxy fallback
python-dotenv>=1.0            # Reads OPENAI_API_KEY from .env

# ── Testing ───────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.23          # Async endpoint testing

# ── Utilities ─────────────────────────────────────────────────────
tqdm>=4.65
